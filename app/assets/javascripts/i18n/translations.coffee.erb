locales = {}

<%
  I18n.backend.send(:init_translations)
  I18n.available_locales.each do |locale|
%>
locales['<%= locale %>'] = {}
locales['<%= locale %>']['translation'] = <%= I18n.backend.send(:translations)[locale.to_sym].to_json %>
<%
  end
%>

window.bundledLocales = locales